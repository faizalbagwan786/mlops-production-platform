# MLOps Production Platform

A modular, production-grade MLOps platform designed to manage the full lifecycle of machine learning models from training to deployment.  
This project focuses on **engineering best practices, automation, and maintainability** rather than model performance alone.

---

## Project Structure


```text
mlops-production-platform/
├── app/                         # FastAPI application for inference
│   ├── main.py
│   ├── model_loader.py
│   ├── schema.py
│   └── model/                   # Generated model artifacts (not versioned)
├── training/                    # Training and evaluation logic
│   ├── train.py
│   └── evaluate.py
├── data/                        # Data storage
│   ├── raw/
│   │   └── churn.csv
│   └── processed/
├── pipelines/                   # Automation & orchestration (future)
├── monitoring/                  # Monitoring & observability (future)
├── docker/                      # Docker configuration
│   └── Dockerfile
├── requirements.txt
└── README.md
```



---

## Goal

Build a **real-world MLOps system** that demonstrates how machine learning models are:

- Trained with reproducibility
- Evaluated before promotion
- Served via a production-ready API
- Containerized for deployment
- Prepared for monitoring, CI/CD, and retraining

---

## Current Milestone

### Milestone 4: Dockerized Inference Service

Completed features:

- End-to-end training pipeline with evaluation gate
- FastAPI-based inference service (`/predict`)
- Input validation using Pydantic schemas
- Model loading at application startup
- Dockerized API runnable locally or on a server
- CI pipeline using GitHub Actions

Model artifacts are generated by the pipeline and **intentionally not committed** to version control.

---

## Tech Stack

- Python
- scikit-learn
- FastAPI
- Docker
- GitHub Actions (CI)
- MLflow (planned)

---

## Getting Started

### 1. Install dependencies

```bash
pip install -r requirements.txt
```

### 2. Train the model

```bash
python training/train.py
```

This will generate the trained model artifact in:

```
app/model/churn_model.pkl
```

### 3. Run the API Locally

Start the FastAPI server:

```bash
uvicorn app.main:app --reload
```

Access Swagger UI:

http://127.0.0.1:8000/docs

---

## API Usage

### Health Check


**GET** `/health`

### Predict Churn

**POST** `/predict`

Request body example:

```json
{
  "age": 30,
  "monthly_charges": 2500,
  "tenure": 12,
  "contract_type": "month-to-month"
}
```

Response example:

```json
{
  "prediction": 1
}
```


---

## Docker Support

The inference service is fully containerized.

**Build the image:**

```bash
docker build -t churn-api -f docker/Dockerfile .
```

**Run the container:**

```bash
docker run -p 8000:8000 churn-api
```

Swagger UI will be available at:

http://localhost:8000/docs

---

## CI/CD

- GitHub Actions pipeline validates the project on each push
- Ensures dependency installation and basic project integrity
- Designed to be extended for tests, linting, and deployment

---

## Design Principles

- Reproducibility over experimentation
- Clear separation of training and inference
- Infrastructure-ready architecture
- Minimal reliance on notebooks
- Incremental, milestone-driven development

---

## Roadmap

Upcoming milestones:

- Model registry and experiment tracking (MLflow)
- Monitoring and data drift detection
- Automated retraining pipelines
- Deployment to cloud infrastructure
- Production-grade CI/CD workflows

---

## Notes

This repository is intended to demonstrate production-oriented MLOps practices.
It is not designed for academic research or experimental notebooks.

---

## License

MIT License

---

### ✅ Next step (recommended)

```bash
git add README.md
git commit -m "Update README with Dockerized MLOps platform details"
git push
```


## Common Issues & Fixes

Below are some common issues encountered during development and deployment, along with their resolutions:

- **PEP 668: Externally Managed Python Environment**  
  On Ubuntu 24.04, global `pip install` is restricted. This was resolved by creating and using a Python virtual environment:
  ```bash
  python3 -m venv venv
  source venv/bin/activate
  pip install -r requirements.txt

500 Internal Server Error – Model Not Loaded
The inference API returned a Model not loaded error when the trained model artifact was missing. Running the training pipeline generated the required model file before starting the API.

Docker Build Context Errors
Docker builds failed when executed from an incorrect directory. This was fixed by running the build command from the project root and ensuring all required files were included in the build context.

These issues are representative of real-world MLOps challenges related to environment management, artifact handling, and containerization.

